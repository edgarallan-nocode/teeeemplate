<h1 style="margin:0 0 16px; font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,Helvetica,Arial,sans-serif; font-size:22px; font-weight:300; letter-spacing:-0.03em; color:#111111;">
  You're in
</h1>

<p style="margin:0 0 16px;">Hello <%= @user.name %>,</p>

<p style="margin:0 0 16px;">
  You have been added to <strong><%= @team.name %></strong> as
  <%= @membership.role_label.downcase %>.
</p>

<%= render "shared/email_button", url: dashboard_url, label: "Open #{@team.name}" %>
