development:
  adapter: async

test:
  adapter: test

# Redis is already a hard dependency for Sidekiq, so Action Cable reuses it
# rather than introducing Solid Cable and a second backing store.
production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL", "redis://localhost:6379/1") %>
  channel_prefix: teeeemplate_production
